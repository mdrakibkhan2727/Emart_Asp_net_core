@model Emart.Models.ViewModels.CategoryVM
@{
    ViewData["Title"] = "Index";
}

<div class="container p-4">
    <div class="card border-light mb-3" style="width:100%">
      <div class="card-header">
             <div class="row p-3">
                <div class="col-6">
                   <h4 class="text-dark">List of Categories</h4>
                </div>
                 <div class="col-6 text-end">
                     <a class="btn btn-primary text-dark" asp-action="CreateUpdate" asp-controller="Category"><i class="bi bi-plus-circle"></i> Create New Category</a>
                </div>
            </div>
      </div>
      <div class="card-body">
            <table class="table table-bordered table-striped" style="width:100%">
              <thead class="text-dark">
                <tr>
                  <th>Category Name</th>
                  <th>Display Order</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                  @foreach (var item in Model.categories)
                  {  
                      <tr>
                          <th>@item.Name</th>
                          <td>@item.DisplayOrder</td>
                          <td role="group">
                             <a asp-action="CreateUpdate" asp-controller="Category" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i></a> 
                             <a asp-action="Delete" asp-controller="Category" asp-route-id="@item.Id"><i class="bi bi-trash"></i></a> 
                          </td>
                      </tr>  
                  }
              </tbody>
         </table>
      </div>
    </div>
</div>

